# Development Docker Compose
# Mounts source code for hot-reload.
# Usage: docker compose up --build
#
# For production, use:  docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  backend:
    image: gam5510/marketscout-backend:latest
    build:
      context: ./backend
      dockerfile: Dockerfile
    expose:
      - "8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    networks:
      - marketscout
    restart: on-failure

  agent-service:
    image: gam5510/marketscout-agent-service:latest
    build:
      context: ./agent-service
      dockerfile: Dockerfile
    expose:
      - "8001"
    env_file:
      - ./agent-service/.env
    volumes:
      - ./agent-service:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8001 --reload
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8001/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - marketscout
    restart: on-failure

  frontend:
    image: gam5510/marketscout-frontend:latest
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_DEMO_MODE: ${NEXT_PUBLIC_DEMO_MODE:-false}
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-ai.itdpy.xyz}
    ports:
      - "5000:5000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    depends_on:
      backend:
        condition: service_healthy
      agent-service:
        condition: service_healthy
    networks:
      - marketscout
    restart: on-failure

networks:
  marketscout:
    driver: bridge
